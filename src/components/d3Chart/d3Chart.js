import React, {PropTypes} from 'react';

const d3Chart = (props) => {
    return (
        <div>

        </div>
    );
};

d3Chart.propTypes = {
    // myProp: PropTypes.string.isRequired
};

export default d3Chart;